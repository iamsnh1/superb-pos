# Tell npm/electron-builder to use the correct binary for the target platform
# This ensures better-sqlite3 and other native modules are rebuilt per target

# Use prebuilt binaries when available, fall back to compiling from source
build_from_source=false
