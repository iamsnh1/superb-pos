name: Build and Release Superb POS

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

# Give the workflow token write access to create releases
permissions:
  contents: write

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false   # Windows won't cancel if Mac fails
      matrix:
        os: [windows-latest, macos-latest]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install dependencies
        run: |
          npm ci
          cd server && npm ci

      - name: Rebuild native modules (better-sqlite3) for Electron
        # This is CRITICAL: better-sqlite3 must be compiled against the exact
        # Electron ABI, NOT the system Node.js ABI, otherwise the server
        # crashes on launch causing 'Server failed to start'.
        run: |
          cd server
          npx @electron/rebuild -f -w better-sqlite3 --electronVersion 40.8.5

      - name: Build frontend
        run: npm run build:frontend

      - name: Build backend server
        run: npm run build:server

      - name: Build and Publish Electron release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          npx electron-builder --publish always
